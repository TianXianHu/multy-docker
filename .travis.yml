sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t positive_thinking/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true positive_thinking/react-test npm test

after_success:
  - docker build -t positive_thinking/multi-client ./client
  - docker build -t positive_thinking/multi-nginx ./nginx
  - docker build -t positive_thinking/multi-server ./server
  - docker build -t positive_thinking/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push positive_thinking/multi-client
  - docker push positive_thinking/multi-nginx
  - docker push positive_thinking/multi-server
  - docker push positive_thinking/multi-worker
